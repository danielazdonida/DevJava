/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
package elografico;

/**
 *
 * @author DanielaDonida
 */
public class Elo  {

    /**
     *
     * @param ielo
     */
    public Elo(int ielo) {
    this.ielo = ielo;
        
    }
    int ielo;
     
   /* 
    public double MenorElo() {
        double menor = 99999;
        for (int k = 0; k < elo; k++) {
            for (int m = 0; m < tempo; m++) {

                double[] line = new double[matrizElos.length];
                double[] row = new double[2];
                line[k] = matrizElos[k][m];
                row[m] = matrizElos[k][0];
                if (row[m] < menor) {
                    menor = row[m];
                    
                }
            }
        }
        
                    return menor;
    }
    
    public double MaiorElo() {
        double maior = 0;

        for (int k = 0; k < elo; k++) {
            for (int m = 0; m < tempo; m++) {

                double[] line = new double[matrizElos.length];
                double[] row = new double[2];
                line[k] = matrizElos[k][m];
                row[m] = matrizElos[k][0];
                if (row[m] > maior) {
                    maior = row[m];

                }
            }
        }
        
        return maior;
    }
    
    public double MaiorTempo() {
        double maior = 0;

        for (int k = 0; k < elo; k++) {
            for (int m = 0; m < tempo; m++) {

                double[] line = new double[matrizElos.length];
                double[] row = new double[2];
                line[k] = matrizElos[k][m];
                row[m] = matrizElos[k][1];
                if (row[m] > maior) {
                    maior = row[m];
   
                }
            }
        }
        
        return maior;
        
    }
    
    public double MenorTempo() {
        double menor = 99999;
        for (int k = 0; k < elo; k++) {
            for (int m = 0; m < tempo; m++) {

                double[] line = new double[matrizElos.length];
                double[] row = new double[2];
                line[k] = matrizElos[k][m];
                row[m] = matrizElos[k][1];
                if (row[m] < menor) {
                    menor = row[m];
                    
                }
            }
        }
        
                    return menor;
    }
*/  
    
}
